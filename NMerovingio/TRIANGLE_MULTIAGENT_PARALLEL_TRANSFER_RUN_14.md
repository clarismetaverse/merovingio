# Triangle Multi-Agent Parallel Transfer — Run 14

> **Execution mode:** CONTROLLED_DRY_RUN  
> **Status:** conceptual experiment, not autonomous software execution  
> **Input network:** canonical `N10` + Run13 candidate strategy only  
> **Hidden proposition:** Euclid I.27 is removed from the available proof rules for this experiment.  
> **Architectural freeze:** no new general strategy is granted in advance. The Run13 candidate may be *retrieved* as a hypothesis, but any broader abstraction must emerge from the run.

---

## 0. Experimental question

Run13 produced the candidate strategy:

```text
CONVERSE_BY_ZERO_AND_ORDER_ELIMINATION
```

by reconstructing I.19 and I.25 from qualitative branch analysis.

Run14 asks whether that strategy transfers outside the side/angle order domain.

We use the transversal/parallel workspace and hide I.27.

Given:

```text
- two distinct straight carriers p and q
- one transversal t crossing both
- alternate-interior angular roles gamma and gamma'
- gamma = gamma'
```

resolve:

```text
closure_topology(p,q) = ?
```

No proposition equivalent to:

```text
alternate-interior equality -> parallel
```

is available.

I.16 remains available as the certified triangle exterior-angle inequality capability.

---

# 1. Search space

Because p and q are explicitly distinct, the relevant qualitative closure states are:

```text
FINITE_CLOSURE_LEFT
DISTRIBUTED_NONCLOSURE
FINITE_CLOSURE_RIGHT
```

These are not encoded as LT/EQ/GT. The experiment therefore tests whether Run13's proof morphology transfers across a different register ontology.

Target register:

```text
C = closure_topology(p,q)
```

with:

```text
C in {
  FINITE_CLOSURE_LEFT,
  DISTRIBUTED_NONCLOSURE,
  FINITE_CLOSURE_RIGHT
}
```

The desired state is withheld from the controlled agents.

---

# 2. Available cognitive resources

### 2.1 Transversal workspace

The material configuration supports:

```text
W_TWO_CARRIERS_TRANSVERSAL
```

with distributed angular role alignment supplied by the shared transversal.

Observed constraint:

```text
rel(gamma,gamma') = EQ
```

This equality is a seed condition for the experiment, not generated by hidden I.27.

### 2.2 Finite-closure host construction

If p and q meet on one side of the transversal, the two carriers plus the transversal generate a triangle host.

The resulting triangle exposes one of gamma/gamma' as an exterior-angle role and the other as an opposite interior role, with orientation determined by which side closes.

### 2.3 I.16

I.16 licenses, in the triangle host:

```text
exterior_angle > opposite_interior_angle
```

Hence finite closure turns the observed equality packet into a strict angular disanalogy.

---

# 3. Controlled agent trace

## Stage A — Genealogy / Compiler retrieval

The network retrieves the Run13 candidate morphology, but only as a search heuristic:

```text
1. enumerate mutually exclusive qualitative source states
2. test non-target alternatives by licensed forward propagation
3. reject only branches that produce certified contradiction
4. synthesize a state only when all alternatives are exhausted
```

Important:

```text
retrieval of strategy != proof license
```

No conclusion is yet made about `C`.

---

## Stage B — Expansion Agent: analyze closure alternatives

The unresolved closure register is expanded into:

```text
Branch L : C = FINITE_CLOSURE_LEFT
Branch N : C = DISTRIBUTED_NONCLOSURE
Branch R : C = FINITE_CLOSURE_RIGHT
```

`Branch N` is held as residual possibility.

No branch is committed.

---

## Stage C-L — left finite-closure counterfactual

Assume:

```text
C = FINITE_CLOSURE_LEFT
```

The workspace can now be rebased to a triangle host:

```text
p + q + t
  -> finite intersection X_left
  -> triangle host T_left
```

Role alignment in this host places the selected alternate-interior roles into an exterior/interior opposition appropriate for I.16.

I.16 licenses a strict inequality:

```text
gamma > gamma'
```

or the orientation-equivalent inequality determined by the chosen naming convention.

But the seed packet requires:

```text
gamma = gamma'
```

Therefore:

```text
STRICT_ORDER collides with EQ
```

and the validator rejects:

```text
REJECT(C = FINITE_CLOSURE_LEFT)
```

Disanalogy genealogy:

```text
finite closure
  -> triangle host
  -> exterior/interior role split
  -> I.16 strict angular disanalogy
  -> collision with distributed angular zero
```

---

## Stage C-R — right finite-closure counterfactual

Assume:

```text
C = FINITE_CLOSURE_RIGHT
```

The symmetric finite closure generates the role-swapped triangle host:

```text
p + q + t
  -> finite intersection X_right
  -> triangle host T_right
```

I.16 again licenses a strict inequality between the same equality-constrained angular roles, now with the opposite orientation under role swap.

Thus:

```text
gamma' > gamma
```

collides with:

```text
gamma = gamma'
```

and the validator rejects:

```text
REJECT(C = FINITE_CLOSURE_RIGHT)
```

---

## Stage D — synthesis of the surviving topology

The closure register is exhaustive under the frozen assumptions:

```text
C in {
  FINITE_CLOSURE_LEFT,
  DISTRIBUTED_NONCLOSURE,
  FINITE_CLOSURE_RIGHT
}
```

and both finite alternatives have been proposition-certified impossible.

Therefore:

```text
C = DISTRIBUTED_NONCLOSURE
```

The human semantic layer may subsequently identify this state as:

```text
parallel carriers
```

but the controlled run does not use the word `parallel` as a hidden target during branch analysis.

This reconstructs the proof content of I.27 without using I.27 as an available rule.

---

# 4. Transfer result

The key question is not merely whether I.27 can be reconstructed.

The important result is that the Run13 morphology transfers from an ordered metric/angular register:

```text
{LT, EQ, GT}
```

to a topological closure register:

```text
{FINITE_LEFT, NONCLOSURE, FINITE_RIGHT}
```

The common pattern is:

```text
UNRESOLVED STATE
  -> enumerate exclusive alternatives
  -> instantiate each counterfactual in a host
  -> propagate by proposition-licensed consequences
  -> reject contradictory branches
  -> recover the unique surviving state
```

Therefore the old Run13 name is now too narrow:

```text
CONVERSE_BY_ZERO_AND_ORDER_ELIMINATION
```

Candidate generalization:

```text
STATE_RECOVERY_BY_COUNTERFACTUAL_EXHAUSTION
```

and candidate family:

```text
F_COUNTERFACTUAL_STATE_RECOVERY
```

This is a candidate only; Run14 does not promote it canonically.

---

# 5. Analysis / synthesis generalization

Run13 suggested:

```text
ANALYSIS
  = expand LT/EQ/GT alternatives

SYNTHESIS
  = recover the surviving qualitative order state
```

Run14 shows the more general form:

```text
ANALYSIS
  = factor an unresolved state into an exhaustive counterfactual partition

PROPAGATION
  = realize each branch in the richest licensed host and propagate consequences

SYNTHESIS
  = reconstruct the state left after certified elimination
```

Thus the method no longer depends on order relations or converses.

---

# 6. Disanalogy momentum in Run14

Run10 transported a break across overlapping families.

Run14 instead starts from a **topological deformation**:

```text
D0 = FINITE_CLOSURE_LEFT | FINITE_CLOSURE_RIGHT
```

Each deformation creates:

```text
closure break
  -> triangle host
  -> angular strict-order packet
  -> collision with angular zero
  -> discharge by rejecting closure branch
```

This is a new momentum type:

```text
TOPOLOGICAL_DISANALOGY
  -> GEOMETRIC_HOST_REALIZATION
  -> ANGULAR_DISANALOGY
  -> ZERO_COLLISION
```

The break changes register domain while preserving genealogy.

---

# 7. Negative controls

The strategy is accepted only under strict exhaustiveness and licensing.

### Negative control A — only one finite direction rejected

If the construction or I.16-role alignment is licensed only for `FINITE_CLOSURE_LEFT`, then:

```text
FINITE_LEFT rejected
FINITE_RIGHT unresolved
NONCLOSURE possible
```

Correct result:

```text
UNRESOLVED {DISTRIBUTED_NONCLOSURE, FINITE_CLOSURE_RIGHT}
```

Forbidden result:

```text
infer DISTRIBUTED_NONCLOSURE
```

### Negative control B — coincidence not excluded

If p and q are not frozen as distinct, a coincidence state may also be geometrically available:

```text
COINCIDENT
```

Then eliminating left/right finite closure does not by itself prove distributed nonclosure.

Correct result:

```text
UNRESOLVED unless COINCIDENT is independently excluded
```

### Negative control C — visual nonintersection

Failure of a finite drawing to show an intersection is not proof of distributed nonclosure.

```text
NO_DRAWN_INTERSECTION != PARALLEL_CERTIFICATION
```

---

# 8. Validator report

## Proposition-certified / conditionally certified

```text
PASS: finite closure creates a triangle host when the carriers are distinct and meet on the hypothesized side
PASS: I.16 forces a strict exterior/interior angular inequality in that host
PASS: strict inequality conflicts with the seeded alternate-angle equality
PASS: the left finite-closure branch is rejected
PASS: the right finite-closure branch is rejected by symmetric role swap
PASS: under the frozen exhaustive topology {left finite, nonclosure, right finite}, nonclosure survives uniquely
```

## Cognitive candidates

```text
CANDIDATE: Run13 and Run14 instantiate one general state-recovery strategy
CANDIDATE: F_CONVERSE_BY_DISANALOGY_EXHAUSTION should generalize to F_COUNTERFACTUAL_STATE_RECOVERY
CANDIDATE: analysis/synthesis is best represented as counterfactual partition + licensed elimination + state reconstruction
CANDIDATE: topological disanalogy can acquire momentum by entering a triangle host and changing register domain
```

## Rejected

```text
REJECT: I.27 is available as a proof rule in this experiment
REJECT: inability to find an intersection counts as proof of nonclosure
REJECT: one rejected finite branch is enough to infer nonclosure
REJECT: an unlicensed or merely visual triangle host can eliminate a branch
REJECT: branch-not-proved equals branch-proved-impossible
```

---

# 9. Experimental result

**Positive transfer result.**

Run14 reconstructs the proof content of I.27 while I.27 is hidden, using I.16 plus exhaustive counterfactual analysis of closure topology.

More importantly, the proof morphology from Run13 transfers across ontology:

```text
Run13:
order state
  -> branch exhaustion
  -> converse order recovery

Run14:
closure topology
  -> branch exhaustion
  -> distributed nonclosure recovery
```

The most important candidate produced by the experiment is therefore:

```text
F_COUNTERFACTUAL_STATE_RECOVERY

ANALYZE
  -> PARTITION STATE SPACE
  -> REALIZE COUNTERFACTUAL HOSTS
  -> PROPAGATE LICENSED CONSEQUENCES
  -> REJECT COLLIDING BRANCHES
  -> SYNTHESIZE SURVIVOR
```

This should not yet be canonicalized. A further transfer or adversarial test should determine whether it is genuinely general or only a coincidence of trichotomic examples.
