# Triangle Multi-Agent Parallelogram Injection into SAS Family — Run 20

> **Execution mode:** CONTROLLED_DRY_RUN  
> **Status:** conceptual experiment, not autonomous software execution  
> **Input network:** canonical `N12` + non-canonical structural candidates from Runs 18A/18B + Run19 comparison context  
> **Architectural freeze:** the parallelogram / double-nonclosure host, `OP_PARTITION_WORKSPACE`, I.27-like parallel certification, I.29-like parallel completion, reflexive side identity, and the SAS congruence capability `P_I4` are available. No theorem asserting equality of opposite sides of a parallelogram is granted in advance.

---

## 0. Experimental question

Inject the same **double-parallel / parallelogram Gestalt** used in Run19 into the SAS family.

Run19 showed that, with diagonal `AC`, the parent host directly generated an ASA packet:

```text
topological zero #1 -> angle EQ #1
topological zero #2 -> angle EQ #2
shared diagonal      -> side EQ
```

Run20 asks whether the same parent host directly generates an SAS zero-mask, and if not, what ontological deficiency is exposed.

A second stage then supplies one independent metric anchor and asks whether SAS can complete the missing topological direction of the parent host.

---

# 1. Frozen parent workspace

Take the same material quadrilateral:

```text
A------B
|      |
|      |
D------C
```

with:

```text
AB || CD
AD || BC
```

and diagonal:

```text
AC
```

Material partition:

```text
T_1 = triangle ABC
T_2 = triangle CDA
```

with:

```text
overlap(T_1,T_2) = AC
AC = CA
```

No equality such as `AB = CD` or `BC = AD` is supplied.

---

# 2. First SAS attempt from topological structure alone

Choose the SAS orientation:

```text
T_1: AB, AC, included angle BAC
T_2: CD, CA, included angle DCA
```

The first parent topological zero:

```text
AB || CD
```

together with transversal `AC` gives via I.29:

```text
angle BAC = angle DCA
```

The shared material bridge gives:

```text
AC = CA
```

So the local packet contains:

```text
side_2 = EQ
included_angle = EQ
side_1 = UNKNOWN
```

where:

```text
side_1 relation = rel(AB,CD)
```

The second parent topological zero:

```text
AD || BC
```

does **not** directly project into:

```text
AB = CD
```

It projects into another angular equality:

```text
angle DAC = angle BCA
```

which is useful for ASA but not the missing SAS metric coordinate in this orientation.

Therefore:

```text
SAS MASK = BLOCKED
```

not rejected.

This is the first main result.

---

# 3. Contrast with Run19 / ASA

Run19:

```text
Z_top_1 -> angle EQ 1
Z_top_2 -> angle EQ 2
shared diagonal -> side EQ
=> ASA complete
```

Run20 initial SAS attempt:

```text
Z_top_1 -> included angle EQ
shared diagonal -> side EQ
Z_top_2 -> another angle EQ
missing: opposite-side metric EQ
=> SAS blocked
```

Hence the same host is **native to ASA but not native to SAS**.

Candidate distinction:

```text
TOPOLOGY_NATIVE_CONGRUENCE_MASK
!=
METRIC_BRIDGE_DEPENDENT_CONGRUENCE_MASK
```

More specifically:

```text
ASA_PARALLELOGRAM_DIAGONAL_NATIVE
SAS_PARALLELOGRAM_DIAGONAL_CONDITIONAL
```

This is not a ranking of criteria. It is a difference in genealogy inside this host topology.

---

# 4. Representational pressure generated by the block

The blocked SAS mask is highly structured:

```text
KNOWN:
  AC = CA
  angle BAC = angle DCA

UNKNOWN:
  AB ? CD
```

So the Expansion/Reorganization Agents do not ask for an arbitrary new theorem.

They isolate exactly one missing coordinate:

```text
METRIC ANCHOR ACROSS THE PARALLEL DIRECTION CLASS
```

The old Run05 pressure reappears in sharper form:

```text
DIRECTION PRESERVATION
+
MAGNITUDE PRESERVATION
-> TRANSLATION-LIKE HOST ?
```

The parallelogram host therefore exposes SAS as a criterion that, in this realization, needs:

```text
one directional/topological zero
+
one shared identity bridge
+
one independent metric transport anchor
```

rather than two directional zeros.

Candidate schema:

```text
SAS_PARALLELOGRAM_REALIZATION =
  TOPOLOGICAL_ZERO
  + SHARED_CARRIER_ZERO
  + TRANSPORTED_METRIC_ZERO
```

---

# 5. Controlled metric-anchor injection

Now add one independent metric fact:

```text
AB = CD
```

Important: this fact is injected as an independent certified metric anchor for this stage.

It is **not** imported from Run19's ASA conclusion and is not assumed merely because the parent is a parallelogram.

Now the local packet is:

```text
AB = CD
AC = CA
angle BAC = angle DCA
```

which licenses `P_I4` / SAS:

```text
triangle BAC congruent triangle DCA
```

or, under the established correspondence:

```text
G_PAIR_COINCIDENCE(T_1,T_2)
```

Thus SAS fires once the metric anchor is supplied.

---

# 6. What SAS returns

Congruence completion now exposes:

```text
BC = AD
angle BCA = angle DAC
angle ABC = angle CDA
```

The key new readout for topology is:

```text
angle BCA = angle DAC
```

In the preserved parent layout, these are alternate interior angles relative to transversal `AC`.

Therefore I.27-like certification can recover:

```text
BC || AD
```

So the trajectory becomes:

```text
KNOWN TOPOLOGICAL ZERO:
  AB || CD

+
INDEPENDENT METRIC ANCHOR:
  AB = CD

+
SHARED DIAGONAL:
  AC = CA

        |
        | I.29
        v

included angle EQ

        |
        | SAS
        v

PAIR CONGRUENCE

        |
        | project corresponding angle
        v

angle BCA = angle DAC

        |
        | I.27
        v

RECOVER SECOND TOPOLOGICAL ZERO:
  BC || AD
```

This is the central positive result of Run20.

---

# 7. SAS as a topological completion bridge

In this realization, SAS does something qualitatively different from ASA.

ASA Run19 used:

```text
two topological zeros
-> two angle equalities
-> congruence
```

SAS Run20 can use:

```text
one topological zero
+ one metric anchor
+ one shared bridge
-> congruence
-> recover the second topological zero
```

So the local congruence criterion mediates between two representational domains:

```text
TOPOLOGY
  -> ANGLE

METRIC
  -> SIDE

SAS
  -> PAIR COINCIDENCE

PAIR COINCIDENCE
  -> NEW ANGLE

NEW ANGLE
  -> TOPOLOGY
```

Candidate capability:

```text
CROSS_DOMAIN_HOST_COMPLETION
```

and more specifically:

```text
SAS_TOPOLOGICAL_COMPLETION_BRIDGE
```

Status: COGNITIVE_CANDIDATE.

---

# 8. Symmetric branch

The same structure can be read with the other side pair.

Start from:

```text
AD || BC
AD = BC
AC = CA
```

I.29 gives:

```text
angle DAC = angle BCA
```

SAS yields pair congruence, which returns:

```text
angle BAC = angle DCA
```

and I.27 can recover:

```text
AB || CD
```

Therefore Run20 discovers two symmetric completion channels:

```text
CHANNEL A:
Z_top(AB,CD)
+ metric EQ(AB,CD)
+ AC identity
-> SAS
-> recover Z_top(AD,BC)

CHANNEL B:
Z_top(AD,BC)
+ metric EQ(AD,BC)
+ AC identity
-> SAS
-> recover Z_top(AB,CD)
```

This produces a new bidirectional family-level picture:

```text
TOPOLOGICAL ZERO A
    + METRIC ANCHOR A
        \
         -> SAS CONGRUENCE
        /
SHARED DIAGONAL
        \
         -> TOPOLOGICAL ZERO B
```

and vice versa by role swap.

---

# 9. Disanalogy behavior

The factorization now makes breaks highly informative.

### Break the topological zero

If:

```text
AB not_parallel CD
```

then:

```text
angle BAC = angle DCA
```

is no longer guaranteed.

Even with:

```text
AB = CD
AC = CA
```

SAS is blocked at the angular coordinate.

### Break the metric anchor

If:

```text
AB ? CD
```

while:

```text
AB || CD
AC = CA
```

remain, SAS is blocked at the non-shared side coordinate.

### Break the shared carrier ownership

If the two triangles do not genuinely share the same material diagonal, role alignment cannot substitute for:

```text
AC = CA
```

and SAS is blocked by the N12 owner-map barrier.

Therefore the three SAS coordinates now have distinct genealogical origins:

```text
included-angle EQ
  <- topological zero projection

shared-side EQ
  <- material identity

other-side EQ
  <- independent metric transport anchor
```

This is a stronger decomposition than a flat SAS mask.

---

# 10. Ontological comparison: ASA vs SAS under the same host

The same double-parallel parent now induces two different local congruence genealogies.

## ASA

```text
topological zero A
topological zero B
shared material bridge
        |
        v
ASA congruence
```

## SAS

```text
topological zero A
metric anchor A
shared material bridge
        |
        v
SAS congruence
        |
        v
topological zero B can be recovered
```

This suggests that congruence criteria should not be stored only as interchangeable masks reaching the same pair-coincidence node.

They also carry different **host-conversion signatures**.

Candidate metadata:

```text
CONGRUENCE_ACCESS_MASK {
  local_zero_pattern
  genealogical_sources
  host_conversion_signature
  completion_affordances
}
```

For this host:

```text
ASA.host_conversion_signature =
  TOPOLOGY + TOPOLOGY + IDENTITY
  -> CONGRUENCE

SAS.host_conversion_signature =
  TOPOLOGY + METRIC + IDENTITY
  -> CONGRUENCE
  -> TOPOLOGY_COMPLETION
```

This is the strongest ontological improvement proposed by Run20.

---

# 11. Validator report

## Stage 1 — un-enriched double-parallel host

```text
PASS: AB || CD -> angle BAC = angle DCA via I.29
PASS: AD || BC -> angle DAC = angle BCA via I.29
PASS: AC = CA by shared material identity
BLOCKED: SAS requires AB = CD or AD = BC, neither follows from topological data alone under the frozen proof licenses
REJECT: parallelism by itself supplies equality of opposite side lengths
```

## Stage 2 — independent metric anchor

```text
PASS: AB = CD + AC = CA + angle BAC = angle DCA licenses SAS
PASS: congruence returns angle BCA = angle DAC
PASS: correctly placed alternate-angle equality licenses BC || AD via I.27
PASS: symmetric channel recovers AB || CD from the opposite side
```

## Guardrails

```text
BLOCKED != REJECTED
TOPOLOGICAL_ZERO != METRIC_ZERO
PARALLELISM DOES NOT IMPLY LENGTH EQUALITY WITHOUT A LICENSED ROUTE
SHARED_ROLE != SHARED_MATERIAL_CARRIER
SAS_CONGRUENCE != PARALLELOGRAM
HOST_CONVERSION_SIGNATURE != THEOREM IDENTITY
```

---

# 12. Main result

Run20 gives both a negative and a positive result.

### Negative

The pure double-parallel Gestalt does **not** directly generate an SAS packet.

This is a genuine structural asymmetry with ASA.

### Positive

With one independent opposite-side metric anchor, SAS becomes a **completion bridge**:

```text
ONE PARALLEL ZERO
+
ONE OPPOSITE-SIDE METRIC ZERO
+
SHARED DIAGONAL
        |
        v
SAS CONGRUENCE
        |
        v
SECOND PARALLEL ZERO
```

Therefore the strongest candidate interpretation is:

```text
ASA is topology-native in the parallelogram-diagonal host.

SAS is topology/metric hybrid and can convert
one directional zero + one metric anchor
into completion of the missing directional zero.
```

This suggests that the family of congruence criteria should be enriched with **genealogical host-conversion signatures**, not flattened into equivalent access masks.

No canonical promotion occurs in Run20.
